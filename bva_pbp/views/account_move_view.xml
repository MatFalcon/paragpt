<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_move_form_inherit_novedades_sen" model="ir.ui.view">
        <field name="name">account.move.form.inherit.novedades.sen</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Novedades SEN">
                    <field name="novedades_sen_ids">
                        <tree>
                            <field name="name"/>
                            <field name="partner_id"/>
                            <field name="cod_negociacion"/>
                            <!-- Add other fields as needed -->
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
