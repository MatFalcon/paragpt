<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

    <!-- Vista para el árbol de cheques propios -->
        <record model="ir.ui.view" id="view_account_check_tree">
            <field name="name">account.check.tree</field>
            <field name="model">account.check</field>
            <field name="priority">100</field>
            <field name="arch" type="xml">
                <tree string="Checks">
                    <field name="checkbook_id" invisible="context.get('default_type', False) != 'issue_check'"/>
                    <field name="journal_id"/>
                    <field name="name" string="Number"/>
                    <field name="issue_date"/>
                    <field name="payment_date"/>
                    <field name="source_partner_id" invisible="context.get('default_type', False) != 'third_check'" string="Emisor"/>
                    <field name="destiny_partner_id" string="Receptor"/>
                    <field name="bank_id" invisible="context.get('default_type', False) != 'third_check'"/>
                    <field name="amount" sum="Total Amount"/>
                    <field name="company_id" groups="base.group_multi_company"/>
                    <field name="state"/>
                    <field name="owner_name" invisible="context.get('default_type', False) != 'third_check'"/>
                </tree>
            </field>
        </record>

        <!-- Vista para el árbol de creación de cheques propios -->

       <record model="ir.ui.view" id="view_account_check_create_tree">
            <field name="name">account.check.tree</field>
            <field name="model">account.check</field>
            <field name="arch" type="xml">
                <tree string="Checks">
                    <field name="checkbook_id" invisible="context.get('default_type', False) != 'issue_check'"/>
                    <field name="journal_id"/>
                    <field name="name" invisible="1" string="Numero"/>
                    <field name="number" string="Numero"/>
                    <field name="issue_date"/>
                    <field name="payment_date"/>
                    <field name="source_partner_id" invisible="context.get('default_type', False) != 'third_check'" string="Emisor"/>
                    <field name="bank_id" invisible="context.get('default_type', False) != 'third_check'"/>
                    <field name="amount" sum="Total Amount"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>

       <!-- Vista de formulario de cheques propios -->
       <record model="ir.ui.view" id="view_account_check_form">
            <field name="name">account.check.form</field>
            <field name="model">account.check</field>
            <field name="arch" type="xml">
                <form string="Checks" create="false">
                    <field name="issue_check_subtype" invisible="1"/>
                    <field name="journal_id" invisible="1"/>
                    <header>
                        <button name="%(action_check_debit)d" type="action" string="Registrar Cobro cheque" context="{'default_check_type': type}" invisible="state != 'signed'" class="oe_highlight"/>
                        <button name="action_sign" type="object" string="Registrar firma de cheque" invisible="state != 'validado'" class="oe_highlight"/>
                        <button name="action_validado" type="object" string="Validar" invisible="state != 'draft'" class="oe_highlight"/>
                        <button name="action_cancel_debit" type="object" string="Cancel Debit" invisible="state != 'debited'" context="{'default_state': state, 'default_type': type}"/>
                        <button name="action_borrador" type="object" string="Marcar como borrador" invisible="state not in ['validado', 'signed', 'handed', 'cancel']"/>
                        <button name="%(action_check_conciliar)d" type="action" string="Conciliar cheque" invisible="type != 'issue_check' or state != 'handed'" class="oe_highlight"/>
                        <button name="action_cancel" type="object" string="Anular" invisible="state != 'draft'"/>
                        <button name="imprimir" type="object" string="Imprimir" class="oe_highlight" invisible="1"/>
                        <field name="state" widget="statusbar" statusbar_colors="{'cancel': 'red', 'cancelado': 'red'}"/>
                    </header>
                    <sheet string="Checks">
                        <h1 invisible="name == False">
                            <field name="name"/>
                        </h1>
                        <group>
                            <group>
                                <field name="type" string="Tipo"/>
                                <field name="checkbook_id" string="Chequera" domain="[('state', '=', 'active')]"/>
                                <field name="bank_id" invisible="type != 'third_check'" required="type == 'third_check'"/>
                                <field name="current_number" string="Ultimo Numero Utilizado" invisible="state not in ['draft', 'validado']"/>
                                <field name="number" string="Numero"/>
                                <field name="currency_id" domain="[('ambito', 'in', ('compra', False))]"/>
                                <field name="amount" string="Monto" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="comentario" string="Comentarios"/>
                                <field name="orden_de"/>
                                <field name="company_currency_amount" invisible="company_currency_amount == 0"/>
                            </group>
                            <group>
                                <field name="owner_name" invisible="type != 'third_check'" required="type == 'third_check'"/>
                                <field name="vat" invisible="type != 'third_check'" required="type == 'third_check'"/>
                                <field name="issue_date" string="Fecha del Cheque"/>
                                <field name="payment_amount" invisible="state not in ['draft', 'validado']"/>
                                <field name="tiene_fact_cred" invisible="1"/>
                                <field name="recibo" invisible="state not in ['signed', 'debited', 'handed']" readonly="state in ['handed', 'conciliado']" required="tiene_fact_cred == True and state == 'signed'"/>
                                <field name="payment_date" string="Fecha de Pago" invisible="type == 'issue_check' and issue_check_subtype == 'currents'" required="type == 'issue_check' and issue_check_subtype == 'deferred'"/>
                                <field name="company_id" groups="base.group_multi_company" widget="selection"/>
                            </group>
                        </group>
                        <group string="Related Documents">
                            <group>
                                <field name="voucher_id" invisible="'from_voucher' in context or state == 'cancel'" readonly="1" context="{'form_view_ref': 'account_voucher.view_vendor_receipt_form'}"/>
                                <field name="third_handed_voucher_id" invisible="'from_voucher' in context or third_handed_voucher_id == False" context="{'form_view_ref': 'account_voucher.view_vendor_payment_form'}"/>
                                <field name="source_partner_id" string="Cliente" invisible="'from_voucher' in context or source_partner_id == False"/>
                                <field name="destiny_partner_id" string="Proveedor" invisible="'from_voucher' in context or destiny_partner_id == False"/>
                            </group>

                            <group>
                                <field name="replacing_check_id" invisible="replacing_check_id == False"/>
                                <field name="debit_account_move_id" string="Asiento Contable" invisible="debit_account_move_id == False"/>
                                <field name="deposit_account_move_id" invisible="deposit_account_move_id == False"/>
                                <field name="customer_reject_debit_note_id" invisible="customer_reject_debit_note_id == False"/>
                                <field name="expense_account_move_id" invisible="expense_account_move_id == False"/>
                                <field name="supplier_reject_debit_note_id" invisible="supplier_reject_debit_note_id == False"/>
                                <field name="return_account_move_id" invisible="return_account_move_id == False"/>
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </field>
        </record>
       <!-- Vista de búsqueda de cheques propios -->
       <record model="ir.ui.view" id="view_checks_search">
            <field name="name">check.search</field>
            <field name="model">account.check</field>
            <field name="arch" type="xml">
                <search string="Checks">
                    <field name="number"/>
                    <field name="source_partner_id"/>
                    <field name="destiny_partner_id"/>
                    <field name="journal_id"/>
                    <field name="voucher_id"/>
                    <field name="company_id" groups="base.group_multi_company"/>
                    <field name="state"/>
                    <field name="owner_name"/>
                    <field name="vat"/>
                    <group expand="0" string="Group By...">
                        <filter name="group_issue_date" string="Fecha de emisión" context="{'group_by':'issue_date'}"/>
                        <filter name="group_payment_date" string="Fecha de pago" context="{'group_by':'payment_date'}"/>
                        <filter name="group_state" string="Estado" context="{'group_by':'state'}"/>
                        <filter name="group_destiny_partner_id" string="Empresa" context="{'group_by':'destiny_partner_id'}"/>
                    </group>
                </search>
            </field>
        </record>
        <!-- Vista de calendario de cheques propios -->
        <record model="ir.ui.view" id="view_account_check_calendar">
            <field name="name">account.check.calendar</field>
            <field name="model">account.check</field>
            <field name="arch" type="xml">
                <calendar string="Checks" date_start="payment_date" color="destiny_partner_id">
                    <field name="amount"/>
                </calendar>
            </field>
        </record>

       <!-- Vista para el árbol de cheques de terceros -->
       <record model="ir.ui.view" id="view_account_third_check_tree">
            <field name="name">account.check.tree</field>
            <field name="model">account.check.third</field>
            <field name="priority">100</field>
            <field name="arch" type="xml">
                <tree string="Checks">
                    <field name="name" string="Number"/>
                    <field name="issue_date"/>
                    <field name="payment_date"/>
                    <field name="source_partner_id" invisible="context.get('default_type', False) != 'third_check'" string="Source Partner"/>
                    <field name="destiny_partner_id" string="Destiny Partner"/>
                    <field name="bank_id" invisible="context.get('default_type', False) != 'third_check'"/>
                    <field name="amount" sum="Total Amount"/>
                    <field name="company_id" groups="base.group_multi_company"/>
                    <field name="state"/>
                    <field name="owner_name" invisible="context.get('default_type', False) != 'third_check'"/>
                </tree>
            </field>
        </record>

        <!-- Vista para el árbol de creación de cheques de terceros -->
       <record model="ir.ui.view" id="view_account_check_third_create_tree">
            <field name="name">account.check.tree</field>
            <field name="model">account.check.third</field>
            <field name="arch" type="xml">
                <tree string="Checks">
                    <field name="journal_id"/>
                    <field name="name" string="Number"/>
                    <field name="issue_date"/>
                    <field name="payment_date"/>
                    <field name="source_partner_id" invisible="context.get('default_type', False) != 'third_check'" string="Source Partner"/>
                    <field name="bank_id" invisible="context.get('default_type', False) != 'third_check'"/>
                    <field name="amount" sum="Total Amount"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>

        <!-- Vista de formulario de cheques de terceros -->
      <record model="ir.ui.view" id="view_account_check_third_form">
            <field name="name">account.check.form</field>
            <field name="model">account.check.third</field>
            <field name="arch" type="xml">
                <form string="Checks Third">
                    <field name="type" invisible="1"/>
                    <field name="journal_id" invisible="1"/>
                    <field name="currency_id" invisible="1"/>
                    <header>
                        <button name="%(action_check_credit)d" type="action" string="Depositar cheque" invisible="state != 'handed'" class="oe_highlight"/>
                        <button name="action_cancel" type="object" string="Anular" invisible="state != 'handed'"/>
                        <button name="action_conciliar" type="object" string="Conciliar" invisible="state != 'deposited'"/>
                        <button name="action_rechazar" type="object" string="Rechazar" invisible="state not in ['handed','deposited']" class="btn btn-danger"/>
                        <button name="action_handed" type="object" string="Pasar en mano" invisible="state not in ['deposited','rechazado']" class="btn btn-warning"/>
                        <field name="state" widget="statusbar" statusbar_colors="{'cancel': 'red', 'cancelado': 'red'}"/>
                    </header>
                    <sheet string="Checks">
                        <h1 invisible="name == False">
                            <field name="name"/>
                        </h1>
                        <group>
                            <group>
                                <field name="number"/>
                                <field name="amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="company_currency_amount" invisible="company_currency_amount == 0"/>
                            </group>
                            <group>
                                <field name="owner_name" invisible="type != 'third_check'" required="type == 'third_check'"/>
                                <field name="issue_date"/>
                                <field name="bank_id"/>
                                <field name="payment_amount" invisible="state not in ['draft', 'validado']"/>
                                <field name="currency_id" domain="[('ambito', 'in', ('venta', False))]"/>
                                <field name="payment_date"/>
                                <field name="company_id" groups="base.group_multi_company" widget="selection"/>
                            </group>
                        </group>
                        <group string="Documentos Relacionados" name="rel">
                            <group>
                                <field name="voucher_id" invisible="'from_voucher' in context or state == 'cancel'" readonly="1" context="{'form_view_ref': 'account_voucher.view_vendor_receipt_form'}"/>
                                <field name="third_handed_voucher_id" invisible="'from_voucher' in context or third_handed_voucher_id == False" context="{'form_view_ref': 'account_voucher.view_vendor_payment_form'}"/>
                                <field name="source_partner_id" invisible="'from_voucher' in context or source_partner_id == False"/>
                                <field name="destiny_partner_id" invisible="'from_voucher' in context or destiny_partner_id == False"/>
                            </group>
                            <group>
                                <field name="replacing_check_id" invisible="replacing_check_id == False"/>
                                <field name="deposit_account_move_id" invisible="deposit_account_move_id == False"/>
                                <field name="customer_reject_debit_note_id" invisible="customer_reject_debit_note_id == False"/>
                                <field name="expense_account_move_id" invisible="expense_account_move_id == False"/>
                                <field name="supplier_reject_debit_note_id" invisible="supplier_reject_debit_note_id == False"/>
                                <field name="return_account_move_id" invisible="return_account_move_id == False"/>
                                <field name="asiento_rechazo" invisible="asiento_rechazo == False"/>
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </field>
        </record>

    <!-- Vista de búsqueda de cheques de terceros -->
      <record model="ir.ui.view" id="view_checks_third_search">
            <field name="name">check.search</field>
            <field name="model">account.check.third</field>
            <field name="arch" type="xml">
                <search string="Checks">
                    <field name="number"/>
                    <field name="source_partner_id"/>
                    <field name="destiny_partner_id"/>
                    <field name="journal_id"/>
                    <field name="voucher_id"/>
                    <field name="company_id" groups="base.group_multi_company"/>
                    <field name="state"/>
                    <field name="owner_name"/>
                    <field name="vat"/>
                    <group expand="0" string="Group By...">
                        <filter name="group_issue_date" string="Fecha de Emision" context="{'group_by':'issue_date'}"/>
                        <filter name="group_payment_date" string="Fecha de Pago" context="{'group_by':'payment_date'}"/>
                        <filter name="group_journal_id" string="Diario" context="{'group_by':'journal_id'}"/>
                        <filter name="group_state" string="Estado" context="{'group_by':'state'}"/>
                        <filter name="group_source_partner_id" string="Emisor" context="{'group_by':'source_partner_id'}"/>
                        <filter name="group_destiny_partner_id" string="Receptor" context="{'group_by':'destiny_partner_id'}"/>
                    </group>
                </search>
            </field>
        </record>

    <!-- Vista de calendario de cheques de terceros -->

       <record model="ir.ui.view" id="view_account_check_third_calendar">
            <field name="name">account.check.calendar</field>
            <field name="model">account.check.third</field>
            <field name="arch" type="xml">
                <calendar string="Cheques de terceros" date_start="payment_date" color="source_partner_id">
                    <field name="amount"/>
                </calendar>
            </field>
        </record>

           <!-- Acción de ventana para cheques de terceros -->
        <record model="ir.actions.act_window" id="action_third_check">
            <field name="name">Third Checks</field>
            <field name="res_model">account.check.third</field>
            <field name="view_mode">tree,form,calendar</field>
            <field name="view_id" ref="view_account_third_check_tree"/>
            <field name="context">{'search_default_state': 'handed', 'default_type': 'third_check'}</field>
        </record>




    <!-- Acción de ventana para cheques emitidos -->
        <record model="ir.actions.act_window" id="action_issue_check">
            <field name="name">Issue Checks</field>
            <field name="res_model">account.check</field>
            <field name="view_mode">tree,form,calendar</field>
            <field name="view_id" ref="view_account_check_tree"/>
            <field name="domain">[('type','=','issue_check')]</field>
            <field name="context">{'search_default_state': 'handed', 'default_type': 'issue_check'}</field>
        </record>




    </data>
</odoo>
