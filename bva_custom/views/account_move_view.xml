<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_move_form_inherit_reclamo_cobro" model="ir.ui.view">
        <field name="name">account.move.form.inherit.reclamo.cobro</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='journal_div']" position="after">
                
                <field name="no_reclamar_cobro" widget="boolean_toggle"/>
                <field name="notificar_retencion" invisible="tipo_comprobante != 27" widget="boolean_toggle"/>
            </xpath>
        </field>
    </record>
</odoo>
